import React from "react";

interface MetaProps {
  title?: string;
  description?: string;
  noindex?: boolean;
}

/**
 * Meta data component
 */

const Meta: React.FC<MetaProps> = (): JSX.Element => {
  return <></>;
};

export default Meta;
