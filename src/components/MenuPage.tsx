import React from "react";
import style from "../styles/modules/MenuPage.module.scss";

const MenuPage: React.FC = (): JSX.Element => {
  return <section className={style["menupage-container"]}></section>;
};

export default MenuPage;
